# Local editor/agent state
.claude/settings.local.json
.DS_Store

# TLC model checker artifacts
tla2tools.jar
states/
*.st

# Go build output (never a bare "krilld" pattern — it swallows cmd/krilld/)
/bin/
/dist/
/krilld
m1-gates/krilld
*.test

# Node (MCP server)
mcp/node_modules/
mcp/dist/

# M2 gate artifacts
m2-gates/results/

# Big binary artifacts that never belong in git
*.ext4
*.img
vmstate
mem
*.log
m1-gates/results/
